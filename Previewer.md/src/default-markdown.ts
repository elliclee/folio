export const DEFAULT_MARKDOWN = `# PreviewerMD Usage Guide

PreviewerMD is a local Markdown editor and previewer. Start writing in the editor, and the preview pane renders the document as you type.

## Quick Start

Open or create Markdown files, or replace this starter document with your own notes.

1. Click **Open Folder** in the sidebar and choose a directory that contains your documents.
2. Select a \`.md\`, \`.markdown\`, or \`.txt\` file from the file tree.
3. Edit the document, then use **Save** to write your changes back to disk.

## Core Features

- **Live preview**: Headings, lists, tables, quotes, and code blocks update while you write.
- **Split view**: Switch between full preview and editor/preview layouts for drafting and review.
- **File tree**: Browse Markdown files inside the current workspace folder.
- **Themes**: Change the reading style from the top toolbar.
- **PDF export**: Export the rendered Markdown document for sharing or archiving.
- **External terminal**: Open the current workspace in your system terminal when you need project commands.

## Markdown Examples

### Task List

- [x] Write the content
- [x] Review the preview
- [ ] Export or save the document

### Table

| Feature | Purpose |
| --- | --- |
| Editor | Write and update Markdown |
| Preview | Inspect the rendered document |
| File tree | Navigate workspace documents |

### Code Block

\`\`\`ts
const title = 'PreviewerMD';
console.log(\`\${title} is ready\`);
\`\`\`
`;
